After a large change, be sure to run pnpm tsc

If changing the database schema, remember to run npx drizzle-kit push

keep client components small and minimal, focused on whatever user interaction needs to be handled

run pnpm lint as well
